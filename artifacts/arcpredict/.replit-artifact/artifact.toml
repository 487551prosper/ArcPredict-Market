kind = "web"
previewPath = "/"
title = "ArcPredict"
version = "1.0.0"
id = "artifacts/arcpredict"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25941

[services.development]
run = "pnpm --filter @workspace/arcpredict run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/arcpredict", "run", "build" ]
publicDir = "artifacts/arcpredict/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25941"
BASE_PATH = "/"
